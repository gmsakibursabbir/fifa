"use client";

// The /watch/[id] route is the same as /watch but with a pre-selected channel.
// The WatchPage component reads the active channel from URL params via useParams().
import WatchPage from "../page";
export default WatchPage;
